name: Socket-CI

on:
  push:
    branches:
      - test-cicd-vitest

permissions:
  contents: read
  packages: write

jobs:
  socket-ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./Socket

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: ./Socket/package-lock.json

      - run: npm ci
      
      - run: npm run build

  cd:
    needs: socket-ci
    uses: ./.github/workflows/socket-cd.yml